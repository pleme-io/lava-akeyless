# lava-akeyless

Autogenerated typed `(deflava-resource …)` shapes for every akeyless
terraform-provider resource. Mirrors pangea-akeyless.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
